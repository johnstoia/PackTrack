name: packtrack
version: 0.3.0
description: Track package shipments and their delivery status (no-auth 17track + mock fallback).
author: jstoia16@gmail.com
provides_tools:
  - shipment_add_tracking
  - shipment_get_status
  - shipment_list_tracked
  - shipment_remove_tracking
